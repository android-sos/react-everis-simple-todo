export React from 'react'

const listItem = (props) => {
    return (
        <div>
            <ul>
            {
                props.tasks.map(()=>{

                })
                
            }
            </ul>
        </div>
    );
}

export default listItem;

